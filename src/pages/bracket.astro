---
import BaseLayout from '../layouts/BaseLayout.astro';
import RadialBracket from '../components/RadialBracket.vue';
---

<BaseLayout title="Bracket Demo" description="Interactive Bracket">
	<main class="wrapper about">
		<section>
			<h2 class="section-title">Radial Bracket</h2>
			<div class="content">
				<p>
					A work‐in‐progress interactive radial bracket for the 2025 March Madness Tournament, built with Vue.js and D3.js. 
					Leveraging Astro’s partial hydration, it delivers a fast, responsive user experience by loading only the essential client‐side code needed for interactivity.
					<br>
					<br>
					Future goals are to:
					<ul>
						<li>Populate the bracket with 2025 teams and corresponding colors.</li>
						<li>Allow for the export/download of the SVG so user's can keep the bracket they create.</li>
						<li>Enhance the bracket to make it more user friendly and "pleasant" to look at.</li>
					</ul>
				</p>
			</div>
		</section>
	</main>
	
    <div class="center-container">
        <div class="responsive-wrapper">
            <!-- Passing a size prop to set the internal drawing dimensions to 1000x1000 -->
            <RadialBracket client:load size={1500} />
        </div>
    </div>
</BaseLayout>

<style>
    .center-container {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        width: 100vw;
    }

    .responsive-wrapper {
        width: 100%;
        height: 100%;
    }

	.about {
		display: flex;
		flex-direction: column;
		gap: 3.5rem;
	}

	section {
		display: flex;
		flex-direction: column;
		gap: 0.5rem;
		color: var(--gray-200);
	}

	.section-title {
		grid-column-start: 1;
		font-size: var(--text-xl);
		color: var(--gray-0);
	}

    .content {
        grid-column: 2 / 4;
    }

    .content :global(a) {
        text-decoration: 1px solid underline transparent;
        text-underline-offset: 0.25em;
        transition: text-decoration-color var(--theme-transition);
    }

    .content :global(a:hover),
    .content :global(a:focus) {
        text-decoration-color: currentColor;
    }


	@media (min-width: 50em) {
		.about {
			display: grid;
			grid-template-columns: 1fr 60% 1fr;
		}

		.about > :global(:first-child) {
			grid-column-start: 2;
		}

		section {
			display: contents;
			font-size: var(--text-lg);
		}
	}
</style>